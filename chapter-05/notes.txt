Chapter 5
----------

docker network create zookafka

docker run -d --name zookeeper --network zookafka -p 2181:2181 wurstmeister/zookeeper

docker run -d --name kafka -p 9092:9092 --network zookafka --env KAFKA_ADVERTISED_HOST_NAME=192.168.99.100 --env KAFKA_ZOOKEEPER_CONNECT=zookeeper:2181 wurstmeister/kafka





playing with it----

in gitbash:

winpty docker exec -it apache_kafka bash
cd opt/bitnami/kafka/bin/

./kafka-topics.sh --bootstrap-server kafka:9092 --create --partitions 3 --replication-factor 1 --topic foo-stream

./kafka-console-producer.sh --broker-list kafka:9092 --topic foo-stream 
hello-world!




ALTER TABLE petclinic.vets
    ADD COLUMN avg_rating real DEFAULT 0;
	
ALTER TABLE petclinic.vets
    ADD COLUMN rating_count integer DEFAULT 0;	